export const metadata = {
  title: "Reset Password",
  description: "Set a new password for your NexInc account.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
